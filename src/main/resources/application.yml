spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/employee_creator}
    username: ${DB_USERNAME:root}
    # password: ${DB_PASSWORD}
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
